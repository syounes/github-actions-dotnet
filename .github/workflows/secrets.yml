name: Secrets workflow

on:
  pull_request:
    branches: [ "master" ]

env:
  API_CSPROJ_PATH: "./GitHubActionsDotNet.Api/GitHubActionsDotNet.Api.csproj"

jobs:
  secrets:
    name: Secrets Demo
    runs-on: ubuntu-latest

    steps:
      - name: echo environment variables
        run: echo "$API_CSPROJ_PATH"
